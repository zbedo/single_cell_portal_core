closeModalSpinner('.modal.fade.in div.spinner-target', '.modal.fade.in', function() {
    $('#notices-target').html("<%= escape_javascript(render partial: '/layouts/notices', locals: {alert: @alert, notice: @notice}) %>");
    enableDefaultActions();
});