$('#wdl-content-target').html("<%= escape_javascript(render partial: 'wdl_content') %>");

$('#wdl-contents').on('show.bs.collapse hide.bs.collapse', function() {
    toggleGlyph($(this).prev().find('span.toggle-glyph'));
});