	var size = $('#scatter-plot').width();

	var scatterLayout = {
		hovermode: 'closest',
		font: plotlyLabelFont,
		width: size,
		height: size,
    <% if @cluster.is_3d? %>
      scene: {aspectmode: 'cube'},
    <% end %>
		xaxis: {
			range: <%= raw @range %>
		},
		yaxis: {
			range: <%= raw @range %>
		}
	};

