	var size = $('#cluster-plot').width() * 0.6;

	var layout = {
		hovermode: 'closest',
		font: plotlyLabelFont,
		// use size * 1.14 to try to square up axes along with legend
		width: size * 1.14,
		height: size,
		xaxis: {
			range: <%= raw @range %>
		},
		yaxis: {
			range: <%= raw @range %>
		}
	}