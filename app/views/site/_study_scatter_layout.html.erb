var size = $('#cluster-plot').width() * SCATTER_RATIO;

var layout = {
  hovermode: 'closest',
  font: plotlyLabelFont,
  width: size,
  height: size,
  xaxis: {
    range: <%= raw @range %>
  },
  yaxis: {
    range: <%= raw @range %>
  },
  legend: {
    x: 100,
    y: 1
  }
}