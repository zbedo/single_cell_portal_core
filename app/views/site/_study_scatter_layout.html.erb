	var size = $('#cluster-plot').width() * 0.7;

	var layout = {
		hovermode: 'closest',
		font: plotlyLabelFont,
		width: size,
		height: size,
		xaxis: {
			range: <%= raw @range %>
		},
		yaxis: {
			range: <%= raw @range %>
		}
	}