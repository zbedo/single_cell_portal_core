var size = $('#cluster-plot').width() * SCATTER_RATIO;

var layout = {
  hovermode: 'closest',
  font: plotlyLabelFont,
  width: size,
  height: size,
  xaxis: {
    range: <%= raw @range %>,
    title: '<%= @axes[:x] %>'
  },
  yaxis: {
    range: <%= raw @range %>,
    title: '<%= @axes[:y] %>'
  },
  legend: {
    x: 100,
    y: 1
  }
}