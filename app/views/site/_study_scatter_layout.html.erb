var size = $('#cluster-plot').width() * SCATTER_RATIO;

var layout = {
  hovermode: 'closest',
  font: plotlyLabelFont,
  width: size,
  height: size,
  xaxis: {
    range: <%= raw @range %>,
    title: '<%= @axes[:x].blank? ? 'X' : @axes[:x] %>'
  },
  yaxis: {
    range: <%= raw @range %>,
    title: '<%= @axes[:y].blank? ? 'Y' : @axes[:y] %>'
  },
  <% if @cluster.cluster_type == '3d' %>
    zaxis: {
      title: 'Z'
    },
  <% end %>
  legend: {
    x: 100,
    y: 1
  }
}