var size = $('#box-plot').actual('width') * SCATTER_RATIO;

if (size < minPlotAreaWidth) {
  size = minPlotAreaWidth;
}


console.log('expression_annotation_scatter')
console.log('size')
console.log(size)

var expressionLayout = {
  hovermode: 'closest',
  font: plotlyLabelFont,
  width: size,
  height: size,
  xaxis:{
    title: '<%= @selected_annotation[:name] %>'
  },
  yaxis: {
    title: '<%= @y_axis_title %>'
  }
}