var size = $('#box-plot').width() * SCATTER_RATIO;

var expressionLayout = {
  hovermode: 'closest',
  font: plotlyLabelFont,
  width: size,
  height: size,
  xaxis:{
    title: '<%= @selected_annotation[:name] %>'
  },
  yaxis: {
    title: '<%= @study.expression_matrix_file.y_axis_label %>'
  }
}