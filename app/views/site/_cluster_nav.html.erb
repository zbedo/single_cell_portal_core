<div class='form-inline navigation-form'>
	<%= label_tag :cluster, 'Select sub-cluster' %><br />
	<%= select_tag :cluster, options_for_select(@options, params[:cluster]), class: 'form-control' %>
</div>
<%= javascript_tag do %>

	$("#cluster").change(function() {
		var url = '<%= render_gene_expression_plots_path(study_name: params[:study_name], gene: params[:gene]) %>';
		var target = document.getElementById('expression-plots');
		opts.top = '10%';
		var spinner = new Spinner(opts).spin(target);
		$(target).data('spinner', spinner);
		var cluster = $('#cluster').val();
		if (cluster == "") {
			$.get(url);
		} else {
			$.get(url + '?cluster=' + cluster);
		}
	});

<% end %>

